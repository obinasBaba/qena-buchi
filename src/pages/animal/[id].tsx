import React from 'react';
import PetDetailScene from '@/scenes/PetDetail';
import { useRouter } from 'next/router';

const PetDetail = () => {
  // access the query params
  return <PetDetailScene />;
};

export default PetDetail;
